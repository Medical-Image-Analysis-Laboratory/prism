# PRISM -- Python dependencies (versions of the environment the paper was run in)
#
# PyTorch must be installed FIRST, from the PyTorch index, so that the CUDA
# build matches your driver (CUDA 11.8 below):
#
#   pip install torch==2.3.0 torchvision==0.18.0 \
#       --index-url https://download.pytorch.org/whl/cu118
#   pip install -r requirements.txt
#   pip install -e .          # builds the two CUDA extensions
#
# See README.md for conda (environment.yml) and Docker alternatives.

# --- deep learning -----------------------------------------------------------
lightning==2.6.1          # also provides lightning-fabric / lightning-utilities
monai==1.3.0              # 1.4+ requires torch>=2.4
torchio==0.20.14

# --- medical image I/O -------------------------------------------------------
nibabel==5.3.2
SimpleITK==2.3.1

# --- numerics ----------------------------------------------------------------
numpy==1.26.4
pandas==2.2.2
scipy==1.15.3

# --- configuration -----------------------------------------------------------
hydra-core==1.3.2
hydra-colorlog==1.2.0
omegaconf==2.3.0
python-dotenv==1.0.0
rootutils==1.0.7

# --- logging / progress / training figures -----------------------------------
rich==13.7.0
tqdm==4.66.4
wandb==0.16.2             # `logger=csv` avoids using it, but it is imported
matplotlib==3.9.3
Pillow==9.3.0

# --- building the CUDA extensions (slice acquisition + transform conversion) --
setuptools==69.5.1
ninja==1.11.1.1

# --- analysis / preprocessing only -------------------------------------------
# Needed by `analysis/build_metrics.py` (rigid registration + SSIM of the volume
# metrics) and `scripts/make_bids_biascorrected.py` (N4). Inference and training
# do not import them.
antspyx==0.4.2
scikit-image==0.22.0

# --- optional ----------------------------------------------------------------
# Only to fetch the released weights from Google Drive (see README):
#   pip install gdown
